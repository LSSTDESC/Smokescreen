name: desc_blinding
channels:
  - conda-forge
dependencies:
    - numpy
    - ipykernel
    - scipy
    - cosmosis >=2.5
    - astropy >=5.3.0
    - pytest
    - pip
    - pip:
      - argparse
      - sacc
      - git+https://github.com/LSSTDESC/firecrown.git@master
    - cosmosis 
    - cosmosis-build-standard-library