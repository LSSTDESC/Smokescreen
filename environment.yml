name: desc_blinding
channels:
  - conda-forge
dependencies:
    - numpy
    - ipykernel
    - scipy
    - cosmosis >=2.5
    - astropy >=5.3.0
    - pytest
    - pip
    - pip:
      - argparse
      - sacc
    - firecrown
    - cosmosis 
    - cosmosis-build-standard-library